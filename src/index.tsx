import * as React from "react";
import * as ReactDOM from "react-dom";
import Routes from "./routes";

const App = (props: any) => <Routes />;

ReactDOM.render(<App />, document.getElementById("root"));
